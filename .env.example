# GitHub personal access token for optional showcase enrichment (stars, last-push date).
# Requires repo:read scope (or public_repo for public repos only).
# In CI this is stored as a repository secret named SHOWCASE_TOKEN.
# Never commit a real token here — this file is safe to commit as-is.
SHOWCASE_TOKEN=
